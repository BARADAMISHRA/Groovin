pipeline{
  agent any
  stages{
    stage("Env variable"){
       steps{
           echo "the current build number is ${env.BUILD_NUMBER}"
           echo "another method is to use ${BUILD_NUMBER}"
       }
    }

  }    
   
}
