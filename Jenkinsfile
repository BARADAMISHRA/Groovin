pipeline{
    stages{
        stage("enviornment"){
            steps{
                echo "Build number is :: ${BUILD_NUMBER}"
                echo "Build number is ${env.BUILD_NUMBER}"
            }
        }
    }
     
}
